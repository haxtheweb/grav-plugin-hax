{% block field %}
	<div class="form-field {{ field.classes|default('') }}">
		<div class="form-data">
			<wysiwyg-hax
				open-default
				element-align="left"
		        offset-margin="45px 0px 0px 0px"
				app-store-connection='{{ appStoreConnection }}'
				field-name="{{ (scope ~ field.name)|fieldName }}"
				class="hax-editor"
			>
			{{ haxSlotArea|raw }}
		  </wysiwyg-hax>
		</div>
	</div>
{% endblock %}
